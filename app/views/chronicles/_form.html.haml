= simple_form_for(@chronicle) do |f|
  = f.error_notification

  .inputs
    = f.input :date
    = f.input :summary
    = f.input :mood, collection: Chronicle::STATES_OF_MIND

  .actions
    = f.button :submit

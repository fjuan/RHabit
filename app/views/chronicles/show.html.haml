%section
  .page-header
    %h1
      = Chronicle.model_name.human
      = @chronicle.date

%p
  %b= Chronicle.human_attribute_name :summary
  = @chronicle.summary
%p
  %b= Chronicle.human_attribute_name :mood
  = t(@chronicle.mood)

%br 

= link_to t('edit'), edit_chronicle_path(@chronicle)
\|
= link_to t('back'), chronicles_path
